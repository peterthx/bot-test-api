# .github/workflows/ci.yml

# 1. Workflow Name
name: CI Pipeline

# 2. Triggers: When to run this workflow
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# 3. Jobs: What tasks to run
jobs:
  build-and-test:
    # 4. Runner: The environment to run the job on
    runs-on: ubuntu-latest

    # 5. Steps: A sequence of tasks
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # Cache pip dependencies

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run Linter (flake8)
        run: flake8 .

      - name: Run Tests (pytest)
        run: pytest


      # # Step 1: Checks out your repository's code
      # - name: Checkout Code
      #   uses: actions/checkout@v4

      # # Step 2: Sets up the specific version of Node.js
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm' # Caches npm dependencies to speed up subsequent runs

      # # Step 3: Installs project dependencies
      # - name: Install Dependencies
      #   run: npm ci

      # # Step 4: Runs linter to check code quality
      # - name: Run Linter
      #   run: npm run lint

      # # Step 5: Runs automated tests
      # - name: Run Tests
      #   run: npm test